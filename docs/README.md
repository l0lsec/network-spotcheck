# GitHub Pages

This folder is the source for the project’s GitHub Pages site.

**To enable:** In the repo go to **Settings → Pages**, set **Source** to “Deploy from a branch”, choose branch `main` (or `master`) and folder **/docs**. Save. The site will be at `https://<username>.github.io/network-spotcheck/`.
